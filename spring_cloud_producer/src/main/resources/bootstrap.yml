spring:
  cloud:
    config:
      name: ciel-config
      profile: dev
      discovery:
        enabled: true
        service-id: spring-cloud-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://10.80.26.71:9001/eureka/